<div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
  <div class="bg-white shadow sm:rounded-lg">
    <div class="px-4 py-5 sm:p-6">
      <h3 class="text-lg leading-6 font-medium text-gray-900 mb-4">
        Subscribe to <%= @plan.name %>
      </h3>
      
      <div class="mb-6 p-4 bg-gray-50 rounded-md">
        <div class="flex justify-between mb-2">
          <span class="text-gray-700">Plan:</span>
          <span class="font-semibold"><%= @plan.name %></span>
        </div>
        <div class="flex justify-between mb-2">
          <span class="text-gray-700">Price:</span>
          <span class="font-semibold"><%= @plan.display_price %></span>
        </div>
        <% if @plan.trial_days > 0 %>
          <div class="flex justify-between">
            <span class="text-gray-700">Trial:</span>
            <span class="font-semibold"><%= @plan.trial_days %>-day free trial</span>
          </div>
        <% end %>
      </div>
      
      <% if ENV['STRIPE_PUBLISHABLE_KEY'].present? && ENV['STRIPE_SECRET_KEY'].present? %>
        <% if @plan.stripe_price_id.present? %>
          <%= form_with url: subscriptions_path, method: :post do |form| %>
            <%= form.hidden_field :plan_id, value: @plan.id %>
            <%= form.submit "Continue to Checkout", 
                class: "w-full bg-indigo-600 text-white px-6 py-3 rounded-md font-semibold hover:bg-indigo-700" %>
          <% end %>
          
          <p class="mt-4 text-sm text-gray-500 text-center">
            You will be redirected to Stripe's secure checkout page.
          </p>
        <% else %>
          <div class="bg-yellow-50 border border-yellow-200 rounded-lg p-6">
            <h4 class="text-yellow-900 font-semibold mb-2">Stripe Price Not Configured</h4>
            <p class="text-yellow-800">
              This plan needs to be configured in Stripe before subscriptions can be purchased.
              Please contact support or configure the Stripe price ID for this plan.
            </p>
          </div>
        <% end %>
      <% else %>
        <div class="bg-amber-50 border border-amber-200 rounded-lg p-6">
          <h4 class="text-amber-900 font-semibold mb-2">Stripe Integration Not Configured</h4>
          <p class="text-amber-800 mb-4">
            To enable subscription purchases, Stripe API keys need to be configured.
          </p>
          <p class="text-amber-800 text-sm">
            If you're the site administrator, please add your Stripe API keys to the environment variables.
          </p>
        </div>
      <% end %>
      
      <div class="mt-6">
        <%= link_to "← Back to Plans", pricing_path, 
            class: "text-indigo-600 hover:text-indigo-900" %>
      </div>
    </div>
  </div>
</div>